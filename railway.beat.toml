[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'python -m celery -A config beat --loglevel=info'"
restartPolicyType = "always"
restartPolicyMaxRetries = 10
