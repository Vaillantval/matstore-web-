[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'python -m celery -A config worker --loglevel=info --concurrency=2 --without-heartbeat'"
restartPolicyType = "always"
restartPolicyMaxRetries = 10
