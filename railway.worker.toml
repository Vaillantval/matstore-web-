[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python -m celery -A config worker --loglevel=info --concurrency=2 --without-heartbeat"
restartPolicyType = "always"
restartPolicyMaxRetries = 10
